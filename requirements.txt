# ─────────────────────────────────────────────────────────────────────────────
# creditscorev4-ml-governance
# Install into a FRESH, project-only venv. Do not reuse a shared/global venv —
# unrelated packages (langchain, crewai, tensorflow, etc.) will produce false
# resolver conflicts even though these pins are internally consistent.
#
# Setup:
#   python -m venv .venv
#   .venv\Scripts\Activate.ps1
#   python -m pip install --upgrade pip setuptools wheel
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# ── CORE ML / DATA ──────────────────────────────────────────────────────────
numpy==1.24.4
pandas==2.0.3
scikit-learn==1.3.0
scipy==1.11.1
xgboost==1.7.6
lightgbm==4.0.0

# ── DATA VALIDATION ──────────────────────────────────────────────────────────
# great_expectations 0.17.15 requires pydantic<2 — this is what forces the
# pydantic v1 chain below (fastapi, httpx). Do not upgrade GX or pydantic
# independently; they must move together.
great_expectations==0.17.15
pandera==0.19.3

# ── DRIFT DETECTION ──────────────────────────────────────────────────────────
evidently==0.4.5
alibi-detect==0.11.4

# ── FAIRNESS & EXPLAINABILITY ────────────────────────────────────────────────
fairlearn==0.8.0
shap==0.42.1
lime==0.2.0.1
aif360==0.5.0

# ── MLOps / EXPERIMENT TRACKING ──────────────────────────────────────────────
# mlflow 2.6.0 requires numpy<2 (satisfied) and works with pydantic v1.
# mlflow 3.x requires pydantic>=2 — do NOT upgrade without also moving off GX 0.17.
mlflow==2.6.0

# NOTE: Airflow and BentoML are NOT installed here. If you actually need
# Airflow, install it separately with its own constraints file — it will
# conflict with several pins above (esp. sqlalchemy, pydantic).
# If you don't need it, delete this note and the sqlalchemy pin comment below.

# ── MONITORING & OBSERVABILITY ───────────────────────────────────────────────
prometheus-client==0.17.1
grafana-api==1.0.3
sentry-sdk==1.29.2

# ── API & SERVING ─────────────────────────────────────────────────────────────
# fastapi/httpx pinned to versions compatible with pydantic v1 (see above).
fastapi==0.103.0
uvicorn==0.23.2
pydantic==1.10.13
httpx==0.24.1

# ── INFRASTRUCTURE & STORAGE ──────────────────────────────────────────────────
# sqlalchemy pinned <2.0 — only strictly required if you add Airflow later.
sqlalchemy==1.4.49
boto3==1.28.25
psycopg2-binary==2.9.7
redis==4.6.0
minio==7.1.15

# ── TESTING ────────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-cov==5.0.0
hypothesis==6.112.0
factory-boy==3.3.1

# ── UTILITIES ──────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
pyyaml==6.0.2
typer==0.9.4
loguru==0.7.2
structlog==24.4.0

# ── DEV TOOLING (no coupling to runtime stack — safe to bump independently) ──
black==24.8.0
isort==5.13.2
flake8==7.1.1
mypy==1.11.2
pre-commit==3.8.0

# ── NOTEBOOK SUPPORT (install separately, not part of app runtime) ───────────
# notebook
# ipykernel